- name: variables
  hosts: localhost
  vars:
   name: "shabeena"
   course: "DEVOPS"
  tasks:
   - name: print info
     ansible.builtin.debug:
      msg: "hi my name is {{name}} im learnig {{course}}" 