- name: conditions
  hosts: [localhost]
  become: yes
  tasks:
  - name: check user exist r not
    ansible.builtin.command: id expense
    register: USER
    ignore_errors: true

  - name: print user info
    ansible.builtin.debug:
     msg: "user info: {{USER}}"

  - name: say hello
    ansible.builtin.debug:
     msg: "hello"
   